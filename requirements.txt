# Telegram Downloader Bot - Python dependencies
# Install with: pip install -r requirements.txt
#
# NOTE: YouTube extraction now requires a JavaScript runtime (deno).
#   Install deno separately: https://docs.deno.com/runtime/getting_started/installation/
#   (on Replit: `npm install -g deno` then ensure it's on PATH)

pyTelegramBotAPI
spotdl
yt-dlp
spotapi
pymongo         # required by spotapi
flask
mutagen
pillow
requests
